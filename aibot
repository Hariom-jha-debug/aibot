# gemini_chatbot.py
from flask import Flask, render_template_string, request, jsonify
import google.generativeai as genai
import markdown
from datetime import datetime
import os

# 🔑 Your API Key (for demo — replace with os.getenv("GOOGLE_API_KEY") in production)
GOOGLE_API_KEY = "AIzaSyDigGJVHQdJcawWiKAkpxlEq7Lx9nkz_vc"

# Configure Gemini
genai.configure(api_key=GOOGLE_API_KEY)
model = genai.GenerativeModel('gemini-pro')

app = Flask(__name__)
chat_history = []

# 🎨 Beautiful UI Template (Qwen/DeepSeek inspired)
HTML_TEMPLATE = '''
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gemini Chatbot</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #8a2be2;
            --primary-light: #a35cff;
            --bg: #0d0d1a;
            --card: #171729;
            --text: #e0e0ff;
            --border: #2d2d44;
            --user-msg: #4ade80;
            --ai-msg: #25253a;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
            background: linear-gradient(135deg, #0d0d1a, #131f33);
            color: var(--text);
            height: 100vh;
            overflow: hidden;
        }

        .chat-container {
            max-width: 900px;
            height: 95vh;
            margin: 20px auto;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 12px 50px rgba(0, 0, 0, 0.6);
            display: flex;
            flex-direction: column;
            background: var(--card);
            border: 1px solid rgba(138, 43, 226, 0.3);
        }

        .chat-header {
            padding: 20px;
            background: linear-gradient(90deg, var(--primary), #5a189a);
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: white;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 14px;
            font-size: 1.6rem;
            font-weight: 700;
        }

        .controls {
            display: flex;
            gap: 12px;
        }

        .controls button {
            background: rgba(255, 255, 255, 0.15);
            border: none;
            width: 44px;
            height: 44px;
            border-radius: 50%;
